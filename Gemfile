source 'http://rubygems.org'
# Add dependencies to develop your gem here.
# Include everything needed to run rake, tests, features, etc.
group :development do
  gem 'mock_redis'
  gem 'rspec'
  gem 'yard'
  gem 'rdoc'
  gem 'bundler'
  gem 'jeweler'

  gem 'guard-rspec'
  gem 'libnotify'
end

group :test do
  gem 'simplecov', :require => false
end

# Use redis orm
gem 'ohm'

# Use rails active model
gem 'activemodel'

# Use background jobs
gem 'resque'

# Use for elasticsearch
gem 'yajl-ruby'

# Use elasticsearch for store and search data
gem 'tire'

# Use universally unique identifiers generator
gem 'uuid'

# Use commands line gem
gem 'thor'

# Use html parser
gem 'nokogiri'

# Use mongo
gem 'mongo'

# Use for mongo
gem 'bson_ext'